<div class="row">
    <div class="col-12 col-md-4 fw-bold">{{ (field.label ?? field.property)|trans }}</div>
    <div class="col-12 col-md-8">
        {%- if data is iterable -%}
            {{ data|serialize }}
        {%- else -%}
            {{ data }}
        {%- endif -%}
    </div>
</div>
