{% if js_assets is defined %}
    {% for asset in js_assets %}
        <script src="{{ asset(asset.value, asset.packageName) }}"></script>
    {% endfor %}
{% endif %}
{% if webpack_encore_assets is defined %}
    {% for entry in webpack_encore_assets %}
        encore_entry_script_tags(entry)
    {% endfor %}
{% endif %}
