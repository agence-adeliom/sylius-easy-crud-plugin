{% extends 'knp_menu.html.twig' %}

{% if 'create' in app.request.attributes.get('_route') %}
    {% set action = 'create' %}
{% else %}
    {% set action = 'update' %}
{% endif %}

{% block list %}
    {% set form = (options.form) %}
    {% set fields = (options.fields) %}
    {% set resource = (options.resource) %}
    {% set menu = (options.menu) %}
    {% set horizontal = (menu.getAttribute('horizontal_display')|default(false)) %}

    <div class="row">
        <div class="col-12{{ horizontal ? '' : ' col-lg-3' }}">
            <div id="side-nav" class="sticky-md-top" style="top: 7.6rem;">
                <div class="list-group{{ horizontal ? ' list-group-horizontal' : '' }} bg-white mb-5" id="crudTab" role="tablist">
                    {% for item in item.children %}
                        {{ block('item') }}
                    {% endfor %}
                </div>
            </div>
        </div>
        <div class="col-12{{ horizontal ? '' : ' col-lg-9' }}">
            <div class="tab-content" id="crudTabContent">
                {% for item in item.children %}
                    {% include item.attribute('template') with {
                        'form': form,
                        'fields': fields,
                        'resource': options.resource,
                        'firstMenuItem': loop.first,
                        'menuItem': item,
                        'view': action
                    } %}
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}

{% block item %}
    {%- set attributes = item.attributes %}
    {%- set classes = item.attribute('class') is not empty ? [item.attribute('class')] : [] %}
    {%- if item.actsLikeFirst %}
        {%- set classes = classes|merge(['active']) %}
    {%- endif %}
    {%- if classes is not empty %}
        {%- set attributes = attributes|merge({'class': classes|join(' ')}) %}
    {%- endif %}
    <button type="button" class="list-group-item list-group-item-action{% if attributes.class is defined %} {{ attributes.class }}{% endif %}" data-bs-toggle="tab" data-bs-target="#{{ item.name }}" role="tab" data-test-side-navigation-tab="{{ item.name }}" aria-selected="true">
        {{ item.label|trans }}
    </button>
{% endblock %}
